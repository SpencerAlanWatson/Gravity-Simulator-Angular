var timestamp,
    lastTimestamp,
    tickDeltas = [],
    clearID = 0;

onmessage = function(e) {
    //postMessage(e.data);
    //timestamp = performance.now();
    //postMessage(timestamp);
    console.count();/*
    if(clearID) {
        clearInterval(clearID);
        clearID = 0;
    } else {
        clearID = setInterval(function(){console.count();}, 0);
        setTimeout(clearInterval.bind(this, clearID), 1000);
    }*/
}